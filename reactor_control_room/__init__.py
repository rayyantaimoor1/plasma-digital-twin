"""Reactor Control Room - a standalone, cinematic companion app for the viva demo
(FUTURE.md item 1).

This package is a SEPARATE companion piece to the graded Streamlit dashboard, not
a replacement for it and not part of the core stack locked in CLAUDE.md. Its only
extra dependencies (fastapi, uvicorn, psutil) live in
`reactor_control_room/requirements.txt`, deliberately kept out of the project's
core `requirements.txt`.
"""
